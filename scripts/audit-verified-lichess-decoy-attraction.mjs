import fs from 'node:fs'; import path from 'node:path'; import readline from 'node:readline'; import { classifyCaptureDecoy } from './lib/verified-lichess-decoy-attraction.mjs'
const root=process.cwd(), local=path.join(root,'.local-verified-lichess-tactics-v1'), input=path.join(root,'audit-reports/verified-lichess-tactics-v1/atomic-m2-structural-v2.ndjson'), output=path.join(local,'decoy-attraction-causal-v1.ndjson'), report=path.join(local,'decoy-attraction-causal-v1-report.json'), lock=path.join(local,'decoy-attraction-causal-v1.lock'); const add=(o,k)=>o[k]=(o[k]??0)+1
if(fs.existsSync(lock))throw new Error('Decoy writer lock exists');if([output,report].some(fs.existsSync))throw new Error('accepted Decoy output exists; do not overwrite it');fs.writeFileSync(lock,`${process.pid}\n`,{flag:'wx'})
try { const state={pipeline:'decoy-attraction-causal-v1',input:0,candidates:0,dispositions:{},samples:{},errors:0,complete:false},writer=fs.createWriteStream(output),source=readline.createInterface({input:fs.createReadStream(input),crlfDelay:Infinity});for await(const line of source){if(!line)continue;state.input+=1;const record=JSON.parse(line);let audit;try{audit=classifyCaptureDecoy(record)}catch(error){state.errors+=1;audit={disposition:'INVALID_GEOMETRY',reason:error.message}}if(!audit)continue;state.candidates+=1;add(state.dispositions,audit.disposition);const bucket=state.samples[audit.disposition]??(state.samples[audit.disposition]=[]);if(bucket.length<100)bucket.push({sourcePuzzleId:record.sourcePuzzleId,displayedFen:record.displayedFen,rating:record.rating,line:record.sourceM2Line,...audit});writer.write(`${JSON.stringify({sourcePuzzleId:record.sourcePuzzleId,canonicalIdentity:record.canonicalIdentity,displayedFen:record.displayedFen,rating:record.rating,sourceM2Line:record.sourceM2Line,audit})}\n`)}await new Promise(resolve=>writer.end(resolve));state.complete=true;fs.writeFileSync(report,`${JSON.stringify(state,null,2)}\n`);console.log(JSON.stringify(state,null,2))}finally{fs.rmSync(lock,{force:true})}
