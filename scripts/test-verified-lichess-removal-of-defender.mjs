import assert from'node:assert/strict';import{Chess}from'chess.js';import{findRemovalOfDefender}from'./lib/verified-lichess-removal-of-defender.mjs';const b=new Chess('k2r4/3q4/8/8/7B/8/8/3RK3 w - - 0 1'),a=new Chess(b.fen()),m=a.move({from:'h4',to:'d8'});assert.ok(m);const f=findRemovalOfDefender(b,a,m,'w')[0];assert.equal(f?.removedDefender,'d8');assert.equal(f?.vulnerableTarget,'d7');console.log('verified Lichess Removal of Defender fixtures passed')
